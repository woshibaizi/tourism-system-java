# Agent SSE streaming service URL (empty = use Vite proxy for dev, must be set for production)
VITE_AGENT_STREAM_URL=
# API base URL (matches Vite proxy target for dev)
VITE_APP_API_URL=/api
# Backend base URL for resolving relative image paths (e.g. uploads/images/xxx.jpg)
VITE_API_BASE_URL=/api
